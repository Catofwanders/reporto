# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Personal report data — generated locally by the pullers and the /email skill.
# Never committed: contains meeting join links and ticket detail.
public/reports/
# Leftover day files from the removed mail views; kept ignored so they cannot be committed.
db/

# Your own settings — copy config.template to config and edit.
config/

# Secrets never belong in git, even without the .local suffix.
.env
.env.*
!.env.example

# Local agent kit (agents, rules, skills) — personal, not published.
.claude/

*storybook.log
storybook-static

# vitest coverage output
coverage/
